let name,
	nameInput = document.getElementById('username-input'),
	toButton = document.getElementById('to-library-button'),
	addButton = document.getElementById('new-library-button');

nameInput.addEventListener('onchange', updateName);
toButton.addEventListener('click', goToLibrary);
newButton.addEventListener('click', newLibary)

let 

function updateName(){
	name = nameInput.value;
}

function goToLibrary(){
	
}

function newLibrary(){

}
